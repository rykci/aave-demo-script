const hre = require("hardhat");

async function getWeth() {
  /**
   * Deposit ETH to mint WETH
   */
}

async function main() {}

main()
  .then(() => process.exit(0))
  .catch((error) => {
    console.error(error);
    process.exit(1);
  });
